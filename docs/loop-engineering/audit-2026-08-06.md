# 方案全面审计报告（2026-08-06）

**审计对象**：`docs/loop-engineering/` 全部 4 份文档（README / integration-plan / patterns-and-levels / implementation-roadmap）
**审计方法**：① 4 份文档交叉比对；② 与真实代码/文件系统事实核查（`src/core/harness.rs`、`scripts/score.sh`、`skills/`、`DOCS.md`、`docs/architecture/north-star-metrics.md` 等）；③ 与 Q1/Q2/Q3 已确认决策一致性检查
**结论**：设计框架成立，但存在 **5 项事实错误、12 项内部矛盾、14 项方案缺口**，全部已在本次修订中修复。

---

## A. 事实性错误（与真实代码不符）

| # | 位置 | 问题 | 证据 | 修订 |
|---|---|---|---|---|
| A1 | integration-plan §4/§4.1 | Maker/Checker 映射自相矛盾：§4 表用 `Verifier` 作 checker，§4.1 注释又说 `Critic` 是 checker | `src/core/harness.rs`：`Verifier` 只判 action 有效性（valid/invalid），`Critic` 才评质量 | 统一为 `Refiner`（maker）+ `Verifier`（gate 1 有效性）+ `Critic`（gate 2 质量）双 gate |
| A2 | integration-plan §1.2 | 写"8 个 skill" | `find skills -name SKILL.md` 实为 **11 个**（goal-md×3、codebase-harness×4、loops×1、utilities×3） | 改为 11，与 roadmap §7 一致 |
| A3 | patterns-and-levels §1.1 | L1 行为写"解析 5 个 component 分" | `scripts/score.sh` 输出 **6 个**：format/clippy/tests/docs/maintenance/safety | 改为 6 个 |
| A4 | patterns-and-levels §3.3 | 写 score.sh 满分 100 | score.sh 满分结构 **110 封顶 100**（20+20+25+15+20+10） | 注明"110 封顶 100，以 `total` 为准" |
| A5 | integration-plan §1.1 | 上游文件 `loop-run-log.md` 与落地文件名 `loop-run-log.jsonl` 无对照说明 | 架构图（§3.1）用 `.jsonl` | 上游清单处标注落地为 `.jsonl` |

## B. 内部矛盾（文档互相冲突）

| # | 冲突双方 | 问题 | 修订 |
|---|---|---|---|
| B1 | patterns §3.1 vs §3.2 | **L0-L3 双重定义**：§3.1 是"Pattern 成熟度"，§3.2 是"loop-doctor 系统 Readiness"，两套都叫 L0-L3，全文混用 | Readiness 改名 **R0-R3**，Pattern 等级保留 L0-L3；全部文档同步 |
| B2 | integration-plan §7 / roadmap 任务 18 / roadmap §4.1（8 维）vs patterns §3.2（10 维） | loop-doctor 维度数 8 vs 10 不一致 | 统一 **10 维**（patterns §3.2 表格求和恰为 100） |
| B3 | roadmap §2 标题"32 个" vs 正文 | 实际 31 新增 + 6 修改（含 `src/main.rs`）= **37 个**；§1 任务表把 main.rs 计入新增且 §2.2 修改清单漏列 | 标题改"31 新增 + 6 修改 = 37"；§2.2 补 `src/main.rs`；任务表第 17 项标注"（修改）" |
| B4 | integration-plan §3.1（`skills/loop/loop-triage/`）vs roadmap §2.1（`skills/loop-triage/`） | skill 目录结构两级 vs 一级 | 统一为现有惯例 **`skills/loops/loop-*`**（category/name/SKILL.md） |
| B5 | gate.yaml denylist `**/Cargo.lock` vs patterns §1.7 "patch 自动 PR" | dependency-watch 自动改 lockfile 与 denylist 冲突 | dependency-watch **只开 PR 不自动改 lockfile**；denylist 加注释 |
| B6 | patterns §1.5 clippy-fmt-watch "自动 commit fix 推到同一 PR" vs allowlist 门禁（无 src/**） | 自动改 `src/**` 绕过 allowlist | 分层：**PR 分支内可自动 commit；allowlist 才可自动 merge**；clippy 失败只评论不自动修 |
| B7 | patterns §6 优先级表引用 `ci-sweeper` / `pr-babysitter` | 这两个 Pattern 不在本项目 7 个之中 | 重写为实际 7 个 Pattern 的优先级 |
| B8 | patterns §1 表 `clippy-fmt-watch` / `dependency-watch` 起始 L2 vs §3.1 解锁条件 | "起始即 L2"绕过 14 天 L1 门禁，与 Q3=A 冲突 | 全部 L1 起步；on-PR 型以触发次数替代天数观察期 |
| B9 | roadmap §4.1 DoD "分数 ≥ 40（L1 解锁）" | Phase 1 是"不开 loop"，不应解锁任何 L | 改为"R1 readiness，工具就绪" |
| B10 | roadmap §3.3 红线"下降超 5 分" vs loop-constraints "不许降低" | 5 分容忍与 0 容忍冲突 | 分层：硬约束 0 容忍（净下降即 fail）+ 红线 >5 分立即终止 |
| B11 | §10 workflow 自动 push vs loop-constraints "不要 push 前不说" | L1 报告 push 是计划内行为但约束未豁免 | 约束加"loop-bot 的 L1 报告 push 是唯一自动例外" |
| B12 | README / patterns §4 信噪比 "≥ 1:3" | 字面 = 1 信 3 噪（差），方向反了 | 改为"需人介入的发现项 ≤ 25%"（越低越好） |

## C. 方案缺口（此前未覆盖）

| # | 缺口 | 修订 |
|---|---|---|
| C1 | `loop-run-log.jsonl` 无行 schema | integration-plan §3.4 新增 JSONL schema 与字段约定 |
| C2 | STATE.md 模板无 Accuracy Tracking 段（Phase 2 必需） | 模板补段 + `L2-unlock-approved` 签字行 |
| C3 | `loop-budget.md` 无模板 | §3.4 新增模板（全局日/月预算、80%/100% 阈值、kill switch） |
| C4 | `LOOP.md` 无模板 | §3.4 新增模板（活动清单 + 心跳表，供 R1 扣分） |
| C5 | CLI 9 个子命令无模块映射，防漂移 | §7 新增"子命令 → 模块"映射表 |
| C6 | 7 个 Pattern 无全局 token 预算汇总 | roadmap §2.3 新增：最坏日 ~125k，全局日预算 150k，超预算按优先级降级 |
| C7 | 外部工具前置（tarpaulin / cargo-audit）未声明 | 风险表 + 边界表补"缺失时对应 Pattern 降级 L1" |
| C8 | "3 次后 escalate"无落点 | workflow `permissions.issues: write` + escalation 进 STATE.md Human Inbox |
| C9 | 失败/重试/flake 策略缺失 | 风险表补 flake 规则（同测试失败 ≥ 2 次才报告） |
| C10 | workflow 无最小权限 | §10 示例加 `permissions: contents/pull-requests/issues: write` |
| C11 | "score 下降自动 revert"无操作细节 | 风险表补：L2 自动 commit 先记基线，下降即 `git revert` 该 commit 并记录 |
| C12 | registry.yaml 示例 `tokens_action: 200000` > `suggested_daily_cap: 100000` | 改为 100000，加"不得超日预算"约束 |
| C13 | AGENTS.md / CLAUDE.md 北极星表未列入修改清单 | roadmap §2.2 注明"可选同步，遵循 AGENTS.md §7.4 指标演进流程" |
| C14 | 时间节点表 Phase 2 只写"Accuracy ≥ 80%" | 改为"Q3=A 四门全过" |

---

## 修订映射

| 文件 | 修订内容 |
|---|---|
| `integration-plan.md` | A1/A2/A5、B1、B2、B4、B5、B6、B11、C1-C4、C5、C8、C10、C12 |
| `patterns-and-levels.md` | A3/A4、B1、B7、B8、B12 |
| `implementation-roadmap.md` | B2、B3、B4、B9、B10、C6、C7、C9、C11、C13、C14 |
| `README.md` | B12、文档地图、当前状态、最后更新 |
| `DOCS.md` | Loop Engineering 索引加本文档 |

## 事实核查依据

- `src/core/harness.rs:21` — `enum HarnessType`（7 变体，Verifier=有效性 / Critic=质量评分 / Refiner=改进）
- `scripts/score.sh:60-65` — 6 component，满分 110 封顶 100
- `find skills -name SKILL.md` — 11 个 SKILL.md
- `DOCS.md` — 已有 Loop Engineering 索引段
- 根目录：`.github/`、`patterns/` 尚不存在（全部为新增文件，方案成立）

## 遗留建议（不阻塞 Phase 1，后续评估）

1. **F-score 替代 accuracy**：patterns §3.4 已预留（漏报权重 2x），建议 Phase 2 积累 ≥ 30 条 review 数据后切换。
2. **分支保护例外**：若 main 开启 branch protection，loop-bot push STATE.md 会失败——需配置路径级例外或使用 PAT（Phase 2 激活前确认）。
3. **`examples/minimal-rust-loop/` 内容**：Phase 1 创建时按"最小可用"原则只含 README.md + SKILL.md（对应 roadmap §2.1 第 30、31 项）即可，gate.yaml 由使用者 fork 后按需复制。
4. **loop-doctor.sh 的自测**：Phase 1 DoD 只要求输出 JSON；建议补一条"分数在改动后不下降"的自回归检查（仿 score.sh 测试范式）。

---

**最后更新**：2026-08-06
